# Database connection (used by Environment.GetEnvironmentVariable("DATABASE_URL"))
DATABASE_URL=Host=postgre;Port=5432;Username=postgres;Password=postgres;Database=ecommerce

# Optional fallback used in configuration (cfg["CONNECTIONSTRING"])
CONNECTIONSTRING=Host=localhost;Port=5432;Username=postgres;Password=postgres;Database=ecommerce

# Kafka bootstrap servers
KAFKA_BOOTSTRAP_SERVERS=kafka:9092